# Local-only environment variables for optional integrations.
# Copy to .env when a local extension needs secrets; do not commit real secrets.

TASK_AGENT_ROOT=/path/to/task-agent
TASK_AGENT_DEFAULT_RUNNER=agent

# Optional multi-agent role prompt repository.
# CODEX_MULTI_AGENT_PROMPTS_REPO=https://github.com/example/agents.git
